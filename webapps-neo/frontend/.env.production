# Used for the Docker build.
#
# Runtime settings are NOT baked into the bundle any more: the image ships
# public/config.json with DOCKER_RUN_PLACEHOLDER_* markers, and the container
# entrypoint (env.sh) substitutes them from the environment given to
# `docker run`. See docs/Docker Build.md.
#
# Nothing needs to be set here.
