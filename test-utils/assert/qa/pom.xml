<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <artifactId>operaton-bpm-assert-qa</artifactId>
  <packaging>jar</packaging>
  
  <name>Operaton - QA - Assert</name>
  
  <parent>
    <groupId>org.operaton.bpm</groupId>
    <artifactId>operaton-bpm-assert-root</artifactId>
    <version>1.0.0-beta-3-SNAPSHOT</version>
  </parent>

  <properties>
    <install.skip>true</install.skip>
  </properties>

  <dependencies>
    <!-- managed by spring-boot-dependencies -->
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
    <!-- managed by operaton internal dependencies -->
    <dependency>
      <groupId>org.operaton.bpm</groupId>
      <artifactId>operaton-engine</artifactId>
      <scope>provided</scope>
    </dependency>
    <dependency>
      <groupId>org.operaton.bpm</groupId>
      <artifactId>operaton-bpm-junit5</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.operaton.bpm</groupId>
      <artifactId>operaton-bpm-assert</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <description>${project.name}</description>
</project>