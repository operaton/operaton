name: Database Migration Test

on:
  pull_request:
    branches:
      - main
      - release/**
    paths:
      - '**/*.sql'
      - 'qa/test-db-rolling-update/**'
  schedule:
    - cron: "15 1 * * 5"     # Runs at 1:15 AM UTC Friday
  workflow_dispatch:

jobs:
  qa:
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        db: [mysql, mariadb, postgres, oracle, sqlserver]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7.0.0

      - name: Set up Docker
        uses: docker/setup-buildx-action@d7f5e7f509e45cec5c76c4d5afdd7de93d0b3df5 # v4.1.0.11.4.1.0

      - name: Run QA script
        run: |
          ./mvnw clean install -DskipTests -PskipFrontendBuild
          cd qa/test-database-container
          ./test.sh ${{ matrix.db }}
