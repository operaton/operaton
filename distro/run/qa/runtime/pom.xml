<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.operaton.bpm.run</groupId>
    <artifactId>operaton-bpm-run-qa</artifactId>
    <version>1.0.0-beta-3-SNAPSHOT</version>
  </parent>

  <artifactId>operaton-bpm-run-qa-runtime</artifactId>
  <name>Operaton - Run - QA - Runtime</name>
  <packaging>jar</packaging>

  <properties>
    <run-home>${project.build.directory}/run/operaton-bpm-run-distro</run-home>
    <example-plugin-home>${project.build.directory}/run/example-plugin</example-plugin-home>
    <install.skip>true</install.skip>
  </properties>

  <dependencies>

    <dependency>
      <groupId>org.operaton.bpm.run</groupId>
      <artifactId>operaton-bpm-run</artifactId>
      <version>${project.version}</version>
      <type>zip</type>
      <scope>test</scope>
      <exclusions>
        <exclusion>
          <!-- exclude transitive artifacts so that they don't
              conflict with the qa runtime versions. -->
          <groupId>*</groupId>
          <artifactId>*</artifactId>
        </exclusion>
      </exclusions>
    </dependency>

  </dependencies>

  <profiles>
    <profile>
      <id>integration-test-operaton-run</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-failsafe-plugin</artifactId>
            <version>2.16</version>
            <configuration>
              <redirectTestOutputToFile>true</redirectTestOutputToFile>
              <dependenciesToScan>
                <dependenciesToScan>org.operaton.bpm.run:operaton-bpm-run-qa-integration-tests</dependenciesToScan>
              </dependenciesToScan>
              <systemPropertyVariables>
                <operaton.run.home>${run-home}</operaton.run.home>
                <example.plugin.home>${example-plugin-home}</example.plugin.home>
                <selenium.screenshot.directory>${project.build.directory}/selenium-screenshots</selenium.screenshot.directory>
              </systemPropertyVariables>
            </configuration>
            <executions>
              <execution>
                <goals>
                  <goal>integration-test</goal>
                  <goal>verify</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-dependency-plugin</artifactId>
            <executions>
              <execution>
                <id>copy-run-distro</id>
                <goals>
                  <goal>unpack-dependencies</goal>
                </goals>
                <phase>generate-test-resources</phase>
                <configuration>
                  <includeArtifactIds>operaton-bpm-run</includeArtifactIds>
                  <overWriteSnapshots>true</overWriteSnapshots>
                  <excludeTransitive>true</excludeTransitive>
                  <outputDirectory>${run-home}</outputDirectory>
                  <excludes>configuration/default.yml,configuration/production.yml</excludes>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-dependency-plugin</artifactId>
            <executions>
              <execution>
                <id>copy-plugin</id>
                <goals>
                  <goal>copy</goal>
                </goals>
                <phase>generate-test-resources</phase>
                <configuration>
                  <artifact>org.operaton.bpm.run:operaton-bpm-run-example-plugin:${project.version}</artifact>
                  <overWriteSnapshots>true</overWriteSnapshots>
                  <stripVersion>true</stripVersion>
                  <outputDirectory>${example-plugin-home}</outputDirectory>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <plugin>
            <groupId>com.googlecode.maven-download-plugin</groupId>
            <artifactId>download-maven-plugin</artifactId>
            <executions>
              <execution>
                <phase>process-resources</phase>
                <goals>
                  <goal>wget</goal>
                </goals>
                <configuration>
                  <url>https://chromedriver.storage.googleapis.com/${version.chromedriver}/chromedriver_${os.type}.zip</url>
                  <outputFileName>chromedriver.zip</outputFileName>
                  <unpack>true</unpack>
                  <outputDirectory>${project.build.directory}/chromedriver</outputDirectory>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>windows</id>
      <activation>
        <os>
          <family>windows</family>
        </os>
      </activation>
      <properties>
        <os.type>win32</os.type>
      </properties>
      <build>
        <pluginManagement>
          <plugins>
            <plugin>
              <groupId>org.apache.maven.plugins</groupId>
              <artifactId>maven-install-plugin</artifactId>
              <configuration>
                <skip>true</skip>
              </configuration>
            </plugin>
          </plugins>
        </pluginManagement>
      </build>
    </profile>
    <profile>
      <id>mac</id>
      <activation>
        <os>
          <family>mac</family>
        </os>
      </activation>
      <properties>
        <os.type>mac64</os.type>
      </properties>
    </profile>
  </profiles>

  <description>${project.name}</description>
</project>
